name: RSI Exit Backtest (Research Only)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: rsi-exit-backtest-research
  cancel-in-progress: false

jobs:
  backtest:
    name: Test 12-month RSI flat-exit confirmation
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout RSI research branch
        uses: actions/checkout@v7
        with:
          ref: rsi-exit-test

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run 12-month rolling RSI exit comparison
        env:
          BACKTEST_FEE_RATE: "0.00045"
        run: |
          python rsi_exit_12m_backtest.py | tee rsi_exit_results.txt

      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rsi-exit-backtest-results
          path: rsi_exit_results.txt
          if-no-files-found: warn
